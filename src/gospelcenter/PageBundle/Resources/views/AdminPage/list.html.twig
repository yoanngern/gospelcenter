{% extends "gospelcenterPageBundle::admin.html.twig" %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('css/admin.css') }}" type="text/css" />
{% endblock %}

{% block title %}
    {{ parent() }} - Pages
{% endblock %}

{% block gospelcenterAdminPages_body %}
    
    <table class="main">
        <thead>
            <tr>
                <th>Title</th>
                <th>Menu</th>
                <th>Language</th>
            </tr>
        </thead>
        <tbody>
            {% for page in pages %}
                <tr>
                    <td><a href="{{ path('gospelcenterAdmin_pages_edit', {'page': page.id, 'center': center.ref}) }}">{{ page.title }}</a></td>
                    <td><a href="{{ path('gospelcenterAdmin_pages_edit', {'page': page.id, 'center': center.ref}) }}">{{ page.ref }}</a></td>
                    <td><a href="{{ path('gospelcenterAdmin_pages_edit', {'page': page.id, 'center': center.ref}) }}">{{ page.language.ref }}</a></td>
                </tr>
            {% else %}
                <tr><td colspan="3">There are no pages.</td></tr>
            {% endfor %}
        </tbody>
    </table>
    
    
{% endblock %}