{% extends "::adminLocalNew.html.twig" %}

{% block classPage %}{{ parent() }} single{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('js/editImage.min.js') }}"></script>
{% endblock %}

{% block title %}
    {{ parent() }} - Edit an image
{% endblock %}

{% block adminTitle %}<h1>Edit an image</h1>{% endblock %}

{% block adminBack %}
    <a class="back" href="{{ path('gospelcenterAdmin_images', {'center': center.ref}) }}">Images</a>
{% endblock %}

{% block adminTools %}
    <a href="{{ path('gospelcenterAdmin_images_delete', {'center': center.ref, 'image': image.id}) }}"
       class="delete">Delete</a>
    <a href="{{ path('gospelcenterAdmin_images', {'center': center.ref}) }}">Cancel</a>
{% endblock %}

{% block adminBody %}

    <form class="image" action="{{ path('gospelcenterAdmin_images_edit', {'center': center.ref, 'image': image.id}) }}" method="post" {{ form_enctype(form) }}>

        {{ form_widget(form.file) }}
        {{ form_errors(form.file) }}
        <div id="editImage" data-input="{{ form.file.vars.id }}">
            <img src="{{ asset(image.webPath) }}" alt="{{ image.alt }}" />
        </div>

        {{ form_widget(form.title) }}
        {{ form_errors(form.title) }}

        {{ form_widget(form.type) }}
        {{ form_errors(form.type) }}

        {{ form_widget(form.description) }}
        {{ form_errors(form.description) }}

        {{ form_rest(form) }}

        <input type="submit" class="buttonLink" value="Save changes"/>
    </form>


{% endblock %}